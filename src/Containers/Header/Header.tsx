import './Header.scss';

const Header = () => {
    return (
        <div className="Header">
            <h1 className="Header__title">Weather App</h1>
        </div>
    );
};

export default Header;
